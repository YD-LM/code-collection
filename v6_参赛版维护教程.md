# v6 竞赛数学 Agent 维护教程

这份教程面向后续维护和参赛调优。它不追求把所有实现细节都讲完，而是回答一个最关键的问题：**以后遇到新问题时，应该改哪个文件，把什么内容放到哪里。**

v6 的核心方向是：

```text
短 routing + 强 prompts + 精准 RAG + 本地工具硬检查 + 轻 verifier + deep 兜底
```

一句话分工：

```text
routing.py 只放短提醒；knowledge_base 放详细方法；math_tools/symbolic_tools 做可机械验证的检查；prompts.py 管模型纪律；user_agent.py 管流程；app.py 管页面。
```

## 1. 为什么 v6 要缩短 routing

之前的 routing 像一个数学百科，内容很全，但会带来几个问题：

- 每次求解都给模型塞很多不一定相关的信息。
- 真正关键的题型结构会被长提示稀释。
- RAG 已经在提供方法，routing 再放长方法会重复。
- prompt 变长后速度变慢，模型也更容易抓不住重点。
- deep 模式会变得越来越像“读很多内容后慢慢猜”。

v6 的做法是：

- routing 只告诉模型“这类题先注意什么”。
- 详细题型方法放进 knowledge_base，由 RAG 按题目检索。
- 能确定对错的检查放进 math_tools 或 symbolic_tools。
- 通用格式和证明纪律放进 prompts。

这样可以减少无效上下文，让 contest 模式更接近参赛默认主力，而不是每道题都依赖 deep。

## 2. 文件职责总览

| 文件或目录 | 负责什么 | 适合放什么 | 不适合放什么 |
|---|---|---|---|
| app.py | 网页入口和结果展示 | 页面标题、模式选择、展示区、下载按钮 | 解题规则、RAG 内容、数学验算 |
| user_agent.py | 主流程控制 | 运行模式、RAG/记忆/校验开关、修正轮数、阶段调用 | 大段数学知识、单题硬编码解法 |
| prompts.py | 模型通用纪律 | JSON 格式、多问全答、证明完整性、verifier 行为 | 某一题完整解法、大型公式库 |
| routing.py | 领域短提醒 | 每个领域 5 到 10 条方向性提醒、领域别名 | 完整教材、长公式表、常见题完整模板 |
| rag.py | 本地知识库检索 | 检索 knowledge_base，并格式化给模型 | 具体数学知识本体，除非是检索逻辑 |
| knowledge_base/*.md | 详细方法库 | 题型模板、常见套路、证明框架、反例构造 | 流程控制代码、API 调用代码 |
| math_tools.py | 本地规则检查 | 字符串可识别、逻辑可判断的硬检查 | 纯口头建议、大段知识讲解 |
| symbolic_tools.py | 符号计算工具 | SymPy 等可计算验证：等价、代点、方程检查 | 题型 prompt、RAG 文本 |
| memory/learned_rules.json | 历史错因记忆 | 以前错过的坑、触发词、简短修正建议 | 详细教材、长证明模板 |
| llm.py | API 客户端 | 模型名、超时、JSON 解析、重试策略 | 数学题型知识 |
| client.py | 底层接口兼容 | 官方 SDK 或 OpenAI 兼容调用包装 | 解题流程和提示词 |

## 3. 失败后固定复盘流程

遇到错题时，不要直接凭印象补规则。固定按下面流程走：

```text
1. 下载 resultXX.json
2. 跑 failure_review.py
3. 看系统建议沉淀到哪一层
4. 你确认
5. 再写入 memory / knowledge_base / math_tools / user_agent
```

命令示例：

```powershell
python failure_review.py "../../result (24).json"
```

相关文件：

- `failure_review.py`：读取 result JSON，输出失败复盘报告；只生成建议，不自动改任何文件。
- `failure_rules.py`：固定失败模式判断规则，例如 verifier 自相矛盾、RAG 未命中、缺少本地工具支持检查。

复盘报告会提示应该沉淀到哪一层：

- `memory/learned_rules.json`：同类错误的短避坑规则。
- `knowledge_base/*.md`：题型方法模板和常见做法。
- `math_tools.py` / `symbolic_tools.py`：能机械验证的检查。
- `prompts.py`：模型通用纪律和 verifier 输出规范。
- `user_agent.py`：流程仲裁、修正策略、运行模式。


如果想把复盘报告保存下来：

```powershell
python failure_review.py "../../result (24).json" -o review_result24.json
```

### failure_review.py 负责什么

`failure_review.py` 是失败复盘入口。它只读取 result JSON 并输出建议，不会自动写入 memory、knowledge_base，也不会改代码。

它会输出：

- 失败类型，例如 `verifier_conflict`、`missing_rag_template`、`missing_local_support_check`。
- 触发词，例如 floor、sum、2^k、解析几何、矩阵等。
- 建议沉淀层级，例如 memory、knowledge_base、math_tools、prompts、user_agent。
- memory 候选规则草稿。
- knowledge_base 候选模板草稿。
- 下一步人工确认事项。

### failure_rules.py 负责什么

`failure_rules.py` 是复盘规则库。以后如果发现新的系统失败模式，优先在这里加 `detect_xxx` 函数。

适合放在这里的规则：

- Verifier 的 counterexample 里 `candidate == expected`，说明它把支持样例误当反例。
- 题目有明显触发词，但 `retrieved_knowledge` 为空，说明 RAG 缺模板。
- 题目有明显触发词，但 `memory_rules` 为空，说明历史避坑没有沉淀。
- 题目适合样例枚举或代回，但 `local_sanity_checks` 为空，说明缺本地工具支持检查。
- 经过多轮 repair 仍未通过，说明修正 prompt 或证据包不够硬。

不适合放在这里的内容：

- 某一道题的完整解答。
- 大段数学教程。
- API 调用逻辑。
- 自动写入 memory 或 knowledge_base 的逻辑。

### 复盘结果如何落地

看到 `recommended_targets` 后按下面规则处理：

| recommended_targets | 应该怎么做 |
|---|---|
| memory | 把 `memory_candidate` 人工确认后写入 `memory/learned_rules.json` |
| knowledge_base | 把 `knowledge_draft` 整理成正式题型模板，放入 `knowledge_base/*.md` |
| math_tools | 新增可机械验证的支持/反驳检查，例如样例表、代回、边界点 |
| symbolic_tools | 把需要 SymPy 的表达式等价、代点、方程残差验证放进去 |
| prompts | 修改通用输出纪律或 verifier 结构化字段要求 |
| user_agent | 修改流程仲裁、repair 策略、运行模式或最终状态判定 |
| app | 只处理页面显示和下载结果，不写数学逻辑 |

这套流程的目的，是把经验沉淀在项目文件里，而不是沉淀在聊天上下文里。即使上下文压缩、换人维护或隔几天再继续，也能通过 result JSON 和复盘报告恢复优化方向。

## 4. 最重要的维护原则

### 原则一：短提醒放 routing

routing.py 只应该像路牌，不应该像教材。

适合写：

```text
解析几何：
- 先列坐标系、已知点线曲线、动点范围和参数范围。
- 点在线、射线、线段、向量关系中先写基点、方向向量和参数限制。
- 动点问题优先求轨迹或参数方程；最值题必须给取等点并代回。
```

不适合写：

```text
椭圆全部定义、焦半径公式、准线公式、通径公式、直线联立所有公式、完整例题推导……
```

如果某领域提示超过几百字，优先考虑拆到 knowledge_base。

### 原则二：新增文件先写清文件职责

以后新增 Python 文件时，文件开头必须用模块 docstring 写清三件事：

```python
"""文件一句话用途。

本文件包括：这里放哪些能力。
本文件控制：它影响哪条流程、哪个入口或哪个判断。
优化时这样操作：以后要加规则、调参数或排错时应该改哪里，不应该把什么塞进来。
"""
```

公共函数也要写清参数和返回值；没有返回值时不写返回部分。这样做的目的不是为了好看，而是让后续队友能快速判断“这段逻辑该不该改、改了会影响哪里”。

### 原则三：详细方法放 knowledge_base

knowledge_base 是 RAG 方法库。它适合放“遇到某类题时，具体怎么做”。

适合新增的文件例子：

```text
knowledge_base/analytic_geometry.md
knowledge_base/function_proof.md
knowledge_base/sequence_inequality.md
knowledge_base/integer_floor_sum.md
knowledge_base/probability_methods.md
knowledge_base/verification_methods.md
```

一个好用的知识片段建议包含：

```markdown
## 题型名称

触发词：椭圆、射线 AP、|AR|·|AP|、最大值

适用场景：
出现动点、轨迹、最值、取等点验证。

标准做法：
1. 明确基点和参数范围。
2. 写出轨迹或约束。
3. 求候选最值。
4. 给出取等点。
5. 代回原题所有条件。

常见错误：
- 把以 A 为起点的射线写成从原点出发。
- 只给最大值，不给取等点。
```

注意：knowledge_base 不是越长越好。每个片段最好有清晰标题和触发词，方便 rag.py 命中。

### 原则四：能机械验证的放 tools

如果一个错误可以由代码判断，就不要只写在 prompt 里提醒模型。

适合放 math_tools.py：

- 多小问是否漏答。
- 某类题最终答案是否明显不可能。
- 几何最值是否缺少取等点。
- 分段函数是否遗漏分段讨论。
- floor/二进制求和题是否只靠样例猜结论。

适合放 symbolic_tools.py：

- 两个表达式是否等价。
- 点是否在轨迹方程上。
- 候选点是否满足约束。
- 简单方程代入是否为 0。
- 多项式恒等式是否成立。

工具检查的原则：

```text
能明确判断错，才返回高风险错误。
只能判断证明不充分，就提示“证明不足”，不要直接判答案错。
```

### 原则五：通用纪律放 prompts

prompts.py 管的是模型的行为底线。

适合写：

- 只输出 JSON。
- 多小问必须全部回答。
- 禁止 response 字段。
- 证明题不能循环论证。
- 最值题必须给取等点和代回验证。
- verifier 判错必须给明确错误点或反例。

不适合写：

- 某一道题的固定答案。
- 某个领域的完整教材。
- 大段公式表。

### 原则六：流程开关放 user_agent

如果你想调速度和准确率，优先看 user_agent.py 的 PIPELINE_MODES。

常用调参位置：

```python
PIPELINE_MODES = {
    "contest": {
        "use_rag": True,
        "rag_top_k": 1,
        "max_hint_chars": 700,
        "max_revisions": 1,
        "thinking_solve": False,
        "repair_failed_only": True,
    }
}
```

如果太慢：

- 降低 rag_top_k。
- 降低 max_hint_chars。
- 关闭 thinking_verify。
- 跳过 explanation。
- 减少 max_revisions。

如果正确率低：

- 给 knowledge_base 增加更好的题型模板。
- 给 math_tools 增加可机械验证的规则。
- contest 的 rag_top_k 从 1 调到 2。
- strict 或 deep 用于失败题兜底。

不要第一反应就把 routing 写长。routing 变长通常只会让每题都慢，不一定让关键题变准。

## 5. v6 的运行模式

### fast

用途：检查接口、页面、JSON 是否正常。

特点：

- 不开 RAG。
- 不开 memory。
- 不开 verifier。
- 不开 explanation。
- 不开 thinking。

适合：刚改完代码后快速确认系统没有报错。

### contest

用途：参赛默认模式。

特点：

- 短 routing。
- 少量 RAG。
- 使用历史记忆。
- 使用本地工具检查。
- verifier 开启，但修正轮有限。
- 默认不开长 thinking。
- 只修失败小问，避免整题重写。

适合：正式做题、批量测试。

### strict

用途：contest 没做对时的重算模式。

特点：

- RAG top_k 更高。
- 领域短提醒上限稍高。
- 仍默认不开长 thinking，避免时间失控。
- 会生成 explanation，方便人工看思路。

适合：需要看详细过程、但不想直接进入 deep 的题。

### deep

用途：兜底慢模式。

特点：

- 允许 solve 和 verify 开 thinking。
- RAG 和 verifier 都开启。
- 修正轮比 contest 多。

适合：contest 和 strict 都失败的难题。deep 不应该成为默认主力。

## 6. 遇到问题时怎么判断该改哪里

### 情况一：模型只做第一问

优先检查：

1. prompts.py 是否要求多小问全答。
2. user_agent.py 的 required_sub_questions 是否正确传给 solver。
3. math_tools.py 是否有多问覆盖检查。
4. app.py 是否只是展示问题，而不是后端没返回。

不要改：routing.py。漏小问通常不是领域知识不足，而是结构约束或流程传参问题。

### 情况二：模型懂题但证明很空

优先改：

1. knowledge_base 中对应题型的方法模板。
2. prompts.py 中证明完整性要求。
3. verifier 的“证明不足”反馈。

必要时加：math_tools.py 的证明结构检查。

### 情况三：模型答案错，但 verifier 没发现

优先改：

1. math_tools.py：能否加硬检查。
2. symbolic_tools.py：能否用 SymPy 代点、化简、验证等价。
3. knowledge_base：补充该类题的标准验证方法。
4. prompts.py：让 verifier 必须给反例或具体错误点。

不要只在 routing.py 加一句提醒。提醒可能被模型忽略，工具检查更可靠。

### 情况四：系统很慢

优先检查：

1. user_agent.py 的 rag_top_k。
2. user_agent.py 的 max_hint_chars。
3. thinking_solve / thinking_verify 是否打开。
4. verifier 是否反复长输出。
5. knowledge_base 片段是否太长。

routing.py 在 v6 已经瘦身，不应该再成为主要慢点。如果还是慢，多半是 RAG、verifier、thinking 或 API 超时。

### 情况五：RAG 检索不到正确方法

优先改 knowledge_base：

- 标题写清楚。
- 加触发词。
- 用题目中常见词表达。
- 一个片段只讲一类方法。

再考虑改 rag.py 的检索算法。

### 情况六：页面显示怪，或下载 JSON 不对

改 app.py。

app.py 只管展示，不应该影响模型计算，除非它传错了模式、修正轮数或 verifier 开关。

## 7. 新增一个题型能力的标准流程

假设你们发现“floor + 二进制求和”经常错，推荐这样加：

### 第一步：knowledge_base 加方法

新增：

```text
knowledge_base/integer_floor_sum.md
```

内容放：

- 触发词。
- 标准二进制位证明。
- 常见错误。
- 验证方法。

### 第二步：routing.py 只加短提醒

如果 number_theory 已经有一句：

```text
floor/二进制/数位和题优先考虑进位、二进制位和数位和恒等式。
```

就不需要继续加长。

### 第三步：math_tools.py 加检查

检查题目是否包含：

- floor 或 \lfloor
- n + 2^k
- 2^{k+1}
- 无穷求和

如果答案不是 n，提示高风险。  
如果答案是 n 但证明没有“二进制位 / 数位和 / s_2(n)”，提示证明不足。

### 第四步：必要时 symbolic_tools.py 加工具

如果能转成表达式验证，就放到 symbolic_tools.py。  
如果只是文本结构检查，放 math_tools.py 即可。

### 第五步：用 contest 测，再用 strict/deep 对照

不要只看一次结果。建议保存 result JSON，对比：

- 总耗时。
- 哪个阶段慢。
- solver 是否答对。
- verifier 是否误判。
- repair 是否真的修了失败点。

## 8. 各文件的优化边界

### app.py

只改用户界面。

可以做：

- 增加模式选择。
- 增加结果展示。
- 增加下载按钮。
- 显示耗时和阶段状态。

不要做：

- 写解题 prompt。
- 写 RAG 知识。
- 写数学验证规则。

### user_agent.py

只改流程。

可以做：

- 调整模式配置。
- 控制是否调用 verifier。
- 控制修正轮数。
- 控制 RAG 和 memory 是否注入。
- 控制失败小问局部修正。

不要做：

- 把某道题答案写死。
- 写长篇数学方法。
- 把工具检查写成散落的 if 判断。

### prompts.py

只改模型行为规范。

可以做：

- 要求 JSON 格式。
- 要求多问全答。
- 要求证明完整。
- 要求 verifier 给具体错误点。

不要做：

- 放完整知识库。
- 放每个领域的百科内容。
- 放某题固定结论。

### routing.py

只改领域短提醒。

可以做：

- 新增领域别名。
- 给某领域加一两条关键提醒。
- 缩短过长的领域提示。

不要做：

- 放完整教材。
- 放一大堆公式。
- 放详细例题。

### knowledge_base

只放可检索方法。

可以做：

- 放题型模板。
- 放常见证明套路。
- 放典型错误。
- 放取等验证方法。

不要做：

- 放流程代码。
- 放 API 设置。
- 放一整个无标题教材。

### math_tools.py

只放本地可判断规则。

可以做：

- 检查漏答。
- 检查高风险错误。
- 检查是否缺少取等点。
- 检查是否缺少关键证明结构。

不要做：

- 大段自然语言讲解。
- 复杂符号计算。
- 模型 prompt。

### symbolic_tools.py

只放符号验证能力。

可以做：

- SymPy 表达式等价。
- 代点验证。
- 轨迹方程验证。
- 方程残差检查。

不要做：

- 题型知识库。
- 页面展示。
- 主流程控制。

## 9. 参赛调优建议

### 日常默认

使用 contest。

目标是速度和正确率平衡。

### 发现题不难但 contest 常错

不要立刻开 deep 作为默认。优先：

1. 看 result JSON，确认错在 solver、verifier 还是 repair。
2. 如果 solver 不知道方法，补 knowledge_base。
3. 如果 solver 算错但工具没发现，补 math_tools/symbolic_tools。
4. 如果 verifier 误判，改 verifier prompt 或降低 verifier 权重。
5. 如果只是展示问题，改 app.py。

### 发现每题都很慢

按顺序排查：

1. 是否 deep 模式。
2. thinking_solve 或 thinking_verify 是否开启。
3. RAG top_k 是否太大。
4. knowledge_base 片段是否太长。
5. verifier 是否长时间枚举。
6. API 是否超时或限速。

### 发现模型常输出非法 JSON

优先改：

- prompts.py 的 JSON 约束。
- llm.py 的 JSON 抽取与修复。
- user_agent.py 的 schema repair。

不要把非法 JSON 问题归因到 routing。

## 10. 最推荐的新增能力顺序

1. 先补 knowledge_base：成本低，收益快。
2. 再补 math_tools：把常错点变成硬检查。
3. 再补 symbolic_tools：把能算的验证交给 SymPy。
4. 再调 prompts：修正模型通用纪律。
5. 最后才改 user_agent 流程。
6. 尽量少改 app，除非是展示或交互问题。

## 11. v6 和 v5plus 的核心区别

| 项目 | v5plus | v6 |
|---|---|---|
| routing | 偏长，像领域百科 | 短提醒，像路牌 |
| RAG | 承担一部分方法补充 | 承担主要详细方法库职责 |
| contest 模式 | 速度略慢，routing 信息较重 | 更轻，依靠短提示 + RAG + 工具 |
| deep 模式 | 容易成为主力 | 只作为兜底 |
| 维护方式 | 容易不知道该往哪加 | 明确文件边界 |

v6 的目标不是“一次改完就满分”，而是让后续优化有清晰方向：

```text
缺方法，补 knowledge_base。
缺硬检查，补 math_tools/symbolic_tools。
缺模型纪律，改 prompts。
缺流程策略，改 user_agent。
缺展示，改 app。
```
